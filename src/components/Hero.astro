---
// Hero.astro
---

<section class="relative min-h-[90vh] flex items-center pt-40 pb-20 overflow-hidden bg-neutral-50 font-sans border-b border-neutral-200">
  {/* Professional Blueprint Background */}
  <div class="absolute inset-0 bg-white"></div>
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_#f8fafc_0%,_#ffffff_100%)]"></div>
  
  {/* Floating Bubbles Pattern */}
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="bubble bubble-1"></div>
    <div class="bubble bubble-2"></div>
    <div class="bubble bubble-3"></div>
    <div class="bubble bubble-4"></div>
    <div class="bubble bubble-5"></div>
    <div class="bubble bubble-6"></div>
    <div class="bubble bubble-7"></div>
    <div class="bubble bubble-8"></div>
  </div>

  {/* Precision Grid Pattern */}
  <div class="absolute inset-0 opacity-[0.03] pointer-events-none">
    <svg width="100%" height="100%" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <pattern id="smallGrid" width="20" height="20" patternUnits="userSpaceOnUse">
          <path d="M 20 0 L 0 0 0 20" fill="none" stroke="#3b82f6" stroke-width="0.5"/>
        </pattern>
        <pattern id="grid" width="100" height="100" patternUnits="userSpaceOnUse">
          <rect width="100" height="100" fill="url(#smallGrid)"/>
          <path d="M 100 0 L 0 0 0 100" fill="none" stroke="#3b82f6" stroke-width="1.5"/>
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#grid)" />
    </svg>
  </div>

  {/* Subtle Grainy Texture for Premium Feel */}
  <div class="absolute inset-0 opacity-[0.40] pointer-events-none mix-blend-soft-light" style="background-image: url('https://www.transparenttextures.com/patterns/stardust.png');"></div>

  <!-- Content layer -->
  <div class="relative z-10 max-w-7xl px-6 w-full text-center mx-auto">
    <div class="max-w-6xl mx-auto space-y-12 animate-fade-in">
      <div class="space-y-4">
        <div class="inline-flex items-center gap-3 px-6 py-2 rounded-full border border-blue-600/20 bg-blue-600/5 backdrop-blur-md">
          <span class="w-2 h-2 rounded-full bg-blue-600 animate-pulse"></span>
          <span class="text-blue-600 text-xs font-black uppercase tracking-[0.4em]">Expert Mobile Car Care</span>
        </div>
      </div>
      
      <h1 class="text-6xl md:text-[6.5rem] font-black text-neutral-900 leading-[1.1] tracking-tighter uppercase drop-shadow-sm">
        PERFECTION IN <br />
        EVERY <span class="text-blue-600 italic">DETAIL</span>.
      </h1>
      
      <p class="text-xl md:text-3xl text-neutral-800 max-w-3xl mx-auto font-light leading-relaxed italic">
        Welcome to <span class="font-bold text-black">Omega Vehicle Detailing</span>. Premium and quality car care brought to you.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-6 pt-6 justify-center">
        <a 
          href="/book-now" 
          class="inline-flex items-center justify-center px-12 py-6 font-black text-white bg-blue-600 transition-all duration-300 hover:bg-neutral-900 shadow-xl shadow-blue-600/10 uppercase tracking-widest text-base rounded-full"
        >
          Book Your Session
        </a>
        
        <a 
          href="/pricing" 
          class="inline-flex items-center justify-center px-12 py-6 font-black text-neutral-900 border-2 border-neutral-200 bg-white transition-all duration-300 hover:border-blue-600 hover:text-blue-600 uppercase tracking-widest text-base rounded-full"
        >
          View Packages
        </a>
      </div>
    </div>
  </div>

  <!-- Scroll Scroll -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 z-10 animate-bounce">
    <div class="w-6 h-10 border-2 border-neutral-300 rounded-full flex justify-center pt-2">
      <div class="w-1 h-2 bg-blue-600 rounded-full"></div>
    </div>
  </div>
</section>

<style>
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeInRight {
    from { opacity: 0; transform: translateX(50px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .animate-fade-in {
    animation: fadeIn 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .animate-fade-in-right {
    animation: fadeInRight 1.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  h1 { font-family: 'Plus Jakarta Sans', sans-serif; }

  .bubble {
    position: absolute;
    bottom: -120px;
    background: radial-gradient(circle at 30% 30%, rgba(59, 130, 246, 0.25), rgba(59, 130, 246, 0.15));
    border: 1px solid rgba(59, 130, 246, 0.3);
    border-radius: 50%;
    animation: float 20s infinite ease-in;
    backdrop-filter: blur(4px);
  }

  .bubble-1 { width: 80px; height: 80px; left: 10%; animation-duration: 25s; animation-delay: 0s; }
  .bubble-2 { width: 120px; height: 120px; left: 25%; animation-duration: 30s; animation-delay: -5s; }
  .bubble-3 { width: 60px; height: 60px; left: 45%; animation-duration: 22s; animation-delay: -2s; }
  .bubble-4 { width: 150px; height: 150px; left: 60%; animation-duration: 35s; animation-delay: -10s; }
  .bubble-5 { width: 40px; height: 40px; left: 80%; animation-duration: 18s; animation-delay: -3s; }
  .bubble-6 { width: 100px; height: 100px; left: 90%; animation-duration: 28s; animation-delay: -7s; }
  .bubble-7 { width: 70px; height: 70px; left: 15%; animation-duration: 26s; animation-delay: -15s; }
  .bubble-8 { width: 130px; height: 130px; left: 75%; animation-duration: 32s; animation-delay: -12s; }

  @keyframes float {
    0% { transform: translateY(0) translateX(0) scale(1); opacity: 0; }
    10% { opacity: 0.95; }
    50% { transform: translateY(-50vh) translateX(50px) scale(1.1); }
    90% { opacity: 0.95; }
    100% { transform: translateY(-120vh) translateX(-20px) scale(1.2); opacity: 0; }
  }
</style>
